[pytest]
asyncio_mode = auto
log_cli=false
log_level=INFO
addopts = --disable-warnings --rootdir=.
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
testpaths =
    tests
